trigger:
- master

pool:
  vmImage: 'ubuntu-latest'
  name: Default
  demands:
    -agent.name -equals DESKTOP-DFGLDAQ

variables:
  buildConfiguration: 'Release'

steps:
- task: UseDotNet@2
- script: dotnet build --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'


